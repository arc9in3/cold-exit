{
  "name": "mission-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-agent workspace — Discord bot orchestrating local + hosted AI models",
  "scripts": {
    "start": "node src/bot.mjs",
    "init-db": "node src/db.mjs --init",
    "newsie": "node src/workers/newsie.mjs",
    "test-ollama": "node -e \"import('./src/util/ollama.mjs').then(m => m.ollamaChat({model:'qwen2.5-coder:7b',messages:[{role:'user',content:'one sentence: what year is it?'}]}).then(r=>console.log(r)))\""
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "better-sqlite3": "^11.5.0",
    "node-cron": "^3.0.3",
    "rss-parser": "^3.13.0",
    "dotenv": "^16.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
